# Каталог: только относительный /api — иначе с HTTPS-сайта запрос на http://… блокируется (mixed content).
# Прокси /api → http://95.154.83.43/api настраивается на хостинге (например vercel.json).
VITE_CATALOG_API_URL=/api
